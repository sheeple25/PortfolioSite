import TracesEntry from "./TracesEntry";

/**
 * The Traces case study rebuilt from the Figma frame `Traces_Attempt`
 * (node 230:10461) — banner, three-margin reading layout, collapsible beats.
 *
 * `/traces-board` still renders the previous translation of the board PDF, so
 * the two arrangements can be compared side by side.
 */
export default function TracesEntryPage() {
  return <TracesEntry />;
}
