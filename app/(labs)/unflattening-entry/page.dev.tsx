import UnflatteningEntry from "./UnflatteningEntry";

/**
 * The Unflattening thesis rebuilt from the Figma frame `Unflattening_Attempt 2`
 * (node 232:12450) — banner, three-margin reading layout, collapsible beats.
 *
 * The Work tab's `/projects/unflattening` still renders the long-form markdown
 * translation of the thesis, untouched, so the two arrangements can be compared
 * side by side. This route is the frame's version: the argument compressed to
 * six beats, with the long version one link away.
 */
export default function UnflatteningEntryPage() {
  return <UnflatteningEntry />;
}
