# TODO

## Done — merged into main (uncommitted)

- [x] Work knowledge graph is back on `/projects` — not above the tiles like before, but as the header's `background` slot (`IndexShell`'s full-viewport texture band behind the masthead). `WorkGraph` gained a `fill` prop: borderless, edge-to-edge, `preserveAspectRatio="xMidYMid slice"` so it bleeds like the slot's other media instead of letterboxing, and its legend/detail/expand-button chrome retints off `--index-header-ground`/`--index-header-ink` instead of `--color-white` so it doesn't render as a white card on the dark ground. The framed 16:9 panel (`fill` unset) is untouched and still available if it's ever wanted elsewhere. Visual design still explicitly not final — this is a first pass at the placement, not a styling pass.
- [x] `IndexShell` gained a `.scrim` layer between the `background` slot and the masthead — a downward gradient off `--index-header-ground` (strong at the top, gone by ~62% down) so title/intro stay legible over whatever's in the slot. Lives in `IndexShell`, not `WorkGraph`, since the same legibility problem would hit any future video/canvas fill too.
- [x] Work graph rebuilt into a real fourth-category map: Project / Domain / Skill / **Tool** (new — see verification item below), covering every project including Archive (already wired, already links out — `handleActivate` in `WorkGraph.tsx` routes archive nodes to `/archive/<slug>`, featured ones to `/projects/<slug>`). Highlights:
  - New `tools:` frontmatter/data field (`lib/writing/types.ts`, `lib/writing/parse.ts`, `lib/caseStudies.ts`) feeds a new `"tool"` graph node type (`lib/graph`), kept separate from `skills:` — a competency and the named software/equipment that produced it aren't the same node.
  - Fixed a real bug: featured and archive project nodes rendered as the *same* colour, because `IndexShell`'s `.page` retints `--color-accent` and `--color-charcoal` to one shared ink token for the rest of the dark-ground chrome. `WorkGraph` now defines its own `--node-featured/-archive/-domain/-skill/-tool` palette (declared on `.frame`, overridden by `.frameFill`) instead of reading the ambient tokens, so the two are visually distinct again (featured = warm highlight) and the legend actually matches what's on screen.
  - Fill mode runs its own coordinate space (`FILL_DIMS` vs `FRAME_DIMS`); force strength/distance are expressed as fractions of `dims.width` (`FORCE_K` in `WorkGraph.tsx`) rather than fixed numbers, so the same tuning behaves correctly at either canvas size instead of only having been tuned for one.
  - Legend/detail/expand chrome (plain HTML, doesn't scale with the SVG) is resized in fill mode — at full-bleed hero scale the old sizing read like a sticker pinned in the corner.
  - Fill mode steps aside below 40rem width (`@media (max-width: 40rem) { .frameFill { display: none; } }`) rather than showing a badly cropped graph on phone-width headers — falls back to the flat header ground, which the masthead was already designed to stand on alone.
  - **Needs your review — drafted tool data, not verified:**
    - Confirmed, not guesses: Traces (`Figma, After Effects, Humanify`) and Loco (`Rhino, Solidworks, Keyshot`) are copied verbatim from their own case-study page `META` rows; Matchbox (`Keyshot`) and Mizan (`Solidworks`) are named directly in their own body text.
    - **Guessed, please correct:** Unflattening → `Figma` (no tools row exists to confirm against anywhere). Flux → `Solidworks` (no tool is named anywhere for it; reused because it's the CAD package already confirmed elsewhere for similar work, not because there's direct evidence). Vortex → left with no `tools:` field at all (hand-craft/braiding work, no software or named equipment evident in the text).
- [x] Legend moved from a top-left bar to a vertical sidebar, and node content redesigned per type:
  - **Bug fix:** the legend used to sit `top-left` inside the header, which in fill mode is *underneath* the site's floating nav bar (the header's background bleeds up behind it on purpose) — the nav bar was clipping it. Now vertically centred on the right edge (`.legend` in `WorkGraph.module.css`), clear of the nav bar up top and the detail panel at bottom-left.
  - **Domain and skill nodes are now their own label text**, not an icon in a circle — `.nodeTagText`/`.nodeTagDomain` in `WorkGraph.module.css`. Domain renders upper-cased/tracked (tag-like), skill in sentence case, so the two stay visually distinct beyond just colour. Font size is `dims.width`-relative (`tagFontSize()`) and the collision/hit radius (`nodeRadius()`) is estimated from each label's actual character count, so long labels ("Structural Prototyping") get proportionally more room than short ones ("CAD") instead of everything sharing one fixed radius.
  - **Tool nodes show the real software logo where one exists**, self-hosted at `public/logos/tools/` (Figma, After Effects, Rhino — from Simple Icons, same pattern as the institution logos already in `public/logos/`), on an opaque light chip so a black-fill mark stays legible on the dark ground. Solidworks, Keyshot and Humanify aren't in that icon set (niche engineering/survey tools) and fall back to a plain initials badge (`SW`, `KS`, `HU`) rather than a fabricated logo — see `TOOL_LOGOS`/`toolIcon()` in `WorkGraph.tsx` if real marks for those ever turn up.
  - **Open — worth a look once it's live:** domain/skill radii are now estimated from label length rather than fixed, and the separation-force constants (`FORCE_K` in `WorkGraph.tsx`) were retuned by reasoning through the math rather than by eye (this session can't watch the live physics settle — see the `feedback_verifying_motion_in_browser` note). With ~40 nodes, several of them long text phrases, some clusters may still read crowded; if so, `FORCE_K.chargeK`/`collidePadK` are the two dials to loosen first.
- [x] Header ground recoloured and the graph's dot-grid turned into an actual background feature:
  - `--index-header-ground`/`--index-header-ink` (`app/globals.css`) moved off the placeholder olive green to a warm near-black (`#17130f`/`#f3ecdf`) — the same family as the site's real dark-mode ground, so the header no longer reads as a mismatched colour if dark mode is toggled while it's on screen. It's still independent of that toggle (declared once at `:root`, not inside the dark-mode block) — a constant hero ground, just one that now shares a palette instead of clashing with it. Checked: no other file hardcoded the old hex values.
  - The dot-grid pattern now fades on two axes via an SVG `<mask>` (`work-graph-dot-mask` in `WorkGraph.tsx`): a horizontal gradient fades it out approaching the left/right edges of the canvas, and a soft radial "clearing" (`nodeRadius(node, dims) * 2.4`) fades it out around every node so the dots don't crowd text/logos. Both live inside the pannable SVG group, same as the dots themselves — panning the graph pans the fades with it rather than them staying pinned to the viewport; a deliberate simplification given this is a passive background texture, not something visitors are expected to drag around.
  - `.frameFill .dot` opacity bumped from 0.4 to 0.55 — at full-bleed hero scale the original placeholder-grid opacity read as barely-there; the framed panel keeps the original quieter value.
